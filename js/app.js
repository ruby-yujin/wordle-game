function appStart() {
  const handleKeydown = (event) => {
    const key = event.key;
    const keyCode = event.keyCode;
  };
  //로직
  window.addEventListener("keydown", handleKeydown);
}

appStart();
